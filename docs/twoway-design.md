# 二刀流 設計規格（工作中）

分支 `twoway`，基底 DEV `18c5bf2`（v2.0.2）。
這份文件是實作前的共識紀錄——**決議**是已經拍板的，**待決**是還沒決定的，
**風險**是我掃過 codebase 之後認為會出事的地方。改動實作時請回頭更新這份。

---

## 0. 主題

平衡。玩家必須同時把投球與打擊撐在水準之上才留得住二刀流資格；
任何一側掉下去就被迫收斂成單刀，而且投在另一側的能力點不會退還。

---

## 1. 決議

| # | 項目 | 決議 |
|---|---|---|
| 1 | 屬性組成 | `sta` ＋投手三項 `vel/ctl/brk` ＋打擊四項 `con/eye/spd/pow`，共 **8 項**。沒有 `rng/fld/arm/cat`。 |
| 2 | 守位 | 不投球的日子固定 **DH**。完全不接守備系統（守位門檻、金手套、守備聖經、移防會議全部跳過）。 |
| 3 | 觸發 A | 遊戲標題點 **7 下** → **強制進入二刀流**（守位選單鎖成單一個「二刀流」），高中三年保送 **3 顆** 6（天才需要 5 顆，剩下兩顆要自己擲）。詳見 §6。 |
| 4 | 觸發 B | **高中時期**解鎖天才時，跳出詢問是否轉二刀流。玩家可以拒絕（拒絕後不再問）。新增那一側的起點＝現有能力平均 × **0.85**。詳見 §6。 |
| 5 | 強制轉回 | 投、打兩側各算一個 ovr；**任一側 < `LV[S.lv].min − 10`** → 強制轉為較好的那一側。 |
| 6 | 轉換起點 | 轉二刀流時新增的屬性：能力＝`max(ri(20,32), 現有平均 × TW_CONVERT_RATIO)`、潛力照 TW 分層表重擲，體力潛力墊到 TW 下限。**原有的守備工具保留**（只是用不到，不是被沒收）。 |
| 7 | 沉沒成本 | 被強制轉回時，投在放棄那一側的能力點**不退還**。 |
| 8 | TJ | 保留。而且復健年**只停投球、打擊照跑**（新的 rehab 路徑）。 |
| 9 | 事件卡 | 有二刀流專屬卡，由專案擁有者在 **XLSX** 補（格式見 §9）。 |
| 10 | 投打配比 | 沿用現有的「開季投球規劃」面板，讓它同時決定**投球場次**。三檔數值見 §5。 |
| 11 | TJ 倍數 | 二刀流專屬：全力投 **1.55** / 普通投 **1.35** / 養生球 **1.20**（單刀是 1.30 / 1.00 / 0.80）。 |
| 12 | 七下的解鎖狀態 | **不寫 localStorage**。每一局都要重點七下；**再連點七下可關閉**，重整頁面亦可。 |
| 13 | 訓練骰 | 二刀流期間**顆數保底 5 顆**（點數保底 4 點由天才提供）；被強制轉回後**顆數保底取消**。詳見 §4。 |
| 14 | 天才連坐 | **七下路線**的玩家一旦失去二刀流身分，**天才一併拔除**（並把 `S.six` 歸零）。有機路線不拔。詳見 §6。 |

### 強制轉回的門檻實際數字

`LV[lv].min − 10`：

| 層級 | min | 二刀流下限 |
|---|---|---|
| 中職二軍 | 30 | 20 |
| 中職一軍 | 41 | **31** |
| 日職二軍 | 44 | 34 |
| 日職一軍 | 50 | **40** |
| 3A | 52 | 42 |
| 大聯盟 | 56 | **46** |

隨層級自動變嚴——升上去了就得兩邊一起跟上，這是「平衡」主題的主要壓力來源。

---

## 2. 待決

- （已結案）兩個入口的強度已經對齊，見 §6 的「轉入起點」。
- 難度旋鈕：`LEAGUE_K.TW` 的整體倍率目前是 **0.785**（名人堂 18%）。
  要調整只要三個聯盟等比縮放，數字表在 `data/economy.js` 的註解裡。

---

## 3. 資料模型

### 為什麼用 `S.pos='TW'` 而不是另開 `S.twoWay` 旗標

全專案有 ~95 個 `S.pos==='P'` 分支散在 15 個檔案。新增第五個值會讓這些分支**明確走錯路**，
容易找、容易測；加旗標的話它們會**安靜地**把二刀流當成打者處理，bug 會延後半年才浮出來。

### 已經支援、不用改的

- `blankStat()`（`state.js:42`）本來就是投打欄位的聯集，`S.stats` **不用改 schema**。
- `S.log[]` 每列同時有 `p`（守位）與 `role`（SP/MR/CL），只是渲染時各挑一個。
- `S.roleYears` 與 `S.dposYears` 兩個都在，只是 `accStat()` 用 `else if` 二選一（要改成兩個都累積）。
- `addIntlStat()`、`capTeam()`、`salaryFor()`、`controlledAnnual()`、`injuryProb()`、`ui/traits.js` 完全與守位無關。

### 要新增的

- `POS_AB.TW = ['sta','vel','ctl','brk','con','pow','spd','eye']`
- `newState()` 的 TW 初始能力／潛力分層表（投手是 4 階、打者是 4 階，TW 要自己一組 8 項的）
- `S.potSum0` 的 TW 門檻（`retire.js:616` 努力仔現在只有投手 237／打者 469 兩個數字）
- 一個獨立的**投球出場計數** `st.GP`（見 §4 與 §8 的風險 #6）

---

## 4. 體力、出賽與訓練骰

**打擊照舊**——不對二刀流額外扣，現有的 `staF` 曲線照跑
（`sta>=55` 才是滿勤 1.0，52 只有 0.94，45 只有 0.72）。
於是體力對二刀流是**雙重門票**：先發線 52、滿勤打擊線 55。這本身就是平衡主題的壓力，不用另外設計。

**投球砍場次、不砍每場局數。** 真實世界的大谷是六人輪值多休一天，
**先發場次少、每場長度正常**（2022 年 28 場先發、平均 5.9 局）。機制上也比較乾淨：
`ipg`（每場局數）是品質訊號，砍了他會看起來像每場被早換的爛投手；
而勝敗、三振、TJ 負荷都跟著場次等比縮，剛好對。

**`st.G` 分家。** `st.G` 留給打擊出賽（所有打者機制原封不動），
投球場次另開 `st.GP`。這一刀同時解掉 §8 的風險 #1（一個欄位兩種意思）
與 #6（TJ 累積被打擊出賽灌爆）。

### 訓練骰：顆數與點數是兩回事

先釐清這兩個維度——遊戲裡本來就分得很清楚，只是容易混：

| 維度 | 程式位置 | 現行規則 | 會動它的屬性 |
|---|---|---|---|
| **顆數**（擲幾顆） | `phases.js:42` | `3 (35%) / 4 (40%) / 5 (20%) / 6 (5%)`，平均 **3.95** | 外務纏身 −1、學院派 35% +1 |
| **點數**（每顆幾點） | `phases.js:47` | `ri(1,6)`，均值 **3.5** | 天才 `ri(4,6)`＝5.0、大器晚成 `ri(3,6)`＝4.5 |

天才的敘述是「訓練骰永久 **4 點**起」——動的是點數，**不碰顆數**。
二刀流是全遊戲**第一個讓顆數保底的機制**。

- **二刀流期間**：`n = max(n, 5)` → 5 (95%) / 6 (5%)，平均 **5.05**。
  不是固定 5 顆，高顆數的運氣照樣吃得到。
- **被強制轉回後**：顆數保底**取消**，回到 3/4/5/6。
  所以保底條件直接綁 `S.pos==='TW'` 即可，**不需要任何常駐旗標**。

每季實得點數（顆數 × 點數均值）：

| 身分 | 顆數保底 | 點數保底 | 平均顆數 | 每顆均值 | 每季點數 |
|---|---|---|---|---|---|
| 一般球員 | — | — | 3.95 | 3.5 | 13.8 |
| 一般天才 | — | 4 點 | 3.95 | 5.0 | 19.8 |
| **二刀流（天才）** | **5 顆** | **4 點** | 5.05 | 5.0 | **25.3** |
| 降回單刀・有機路線 | — | 4 點 | 3.95 | 5.0 | **19.8** |
| 降回單刀・七下路線 | — | — | 3.95 | 3.5 | **13.8** |

兩條落點都剛好回到既有檔位：有機路線跌回去＝一個普通的天才，
七下路線跌回去＝一個普通球員——而且是一個**已經把點數浪費在另一側**的普通球員。
不必另外設計懲罰，回到原點本身就是懲罰。

**要留給第 3 階段驗的**：二刀流的 25.3 點攤在 8 項上是 3.16 點／項，
一般天才投手是 19.8 攤在 4 項（4.94 點／項）、天才打者是 19.8 攤在 8 項（2.47 點／項）。
所以二刀流真正的競爭對手是**打者路線**，不是投手路線；模擬要拿它跟天才打者比，不是跟天才投手比。

---

## 5. 投打配比：沿用「開季投球規劃」面板

現有的 `投球規劃`（全力投／普通投／養生球 → `S.effort`）掛在 `phases.js:85-94`，
目前只餵給 `season.js`（`st.d ±1`、ERA ∓0.25、三振 ±6%）與 `injury.js` 的 `tjAccrue`（TJ 倍率）
——**完全不影響場次**。二刀流讓它同時決定投球場次，就不必新開一張面板。

| 檔位 | 先發場次 | 打擊出賽 | TJ 倍數（單刀） |
|---|---|---|---|
| 全力投 | **85%** | **90%** | 1.55（1.30） |
| 普通投 | **70%** | 全勤 | 1.35（1.00） |
| 養生球 | **50%** | 全勤 | 1.20（0.80） |

（`st.d`／ERA／三振的既有效果不動。）

### 為什麼獎項門檻不用另外打折

`年度最佳投手` 與 `防禦率王` 的資格是硬門檻 `st.IP >= th.g`（中職 120／日職 143／大聯盟 162 局）。
單刀投手的局數乘上三檔之後：

| 聯盟 | 體力 | 單刀 IP | 全力投 | 普通投 | 養生球 | 門檻 |
|---|---|---|---|---|---|---|
| 中職一軍 | 60 | 151 | **128 ✓** | 106 | 75 | 120 |
| 中職一軍 | 80 | 187 | **159 ✓** | **131 ✓** | 93 | 120 |
| 日職一軍 | 70 | 167 | 142 | 117 | 84 | 143 |
| 日職一軍 | 80 | 186 | **158 ✓** | 130 | 93 | 143 |
| 大聯盟 | 60 | 204 | **173 ✓** | 142 | 102 | 162 |
| 大聯盟 | 70 | 227 | **193 ✓** | 159 | 114 | 162 |

**只有全力投摸得到門檻**，於是「今年要拚最佳投手」變成一個要付手臂代價的主動選擇，
而不是系統送的被動折扣。日職最硬（要體力 80 + 全力投），與 `awards.js` 裡
「六人輪值讓日職的量產型獎項門檻相對難」的既有註解一致，不另開後門。

### TJ 倍數為什麼比單刀高

`tjAccrue()` 的 workload 項是 `IP ÷ 該層級場次`。局數砍下去，這一項會**自動**讓二刀流的
手臂比全職王牌更耐用——但現實相反（大谷 23 歲、29 歲各開一次刀）。
把倍數拉到 1.45 / 1.25 / 1.10 之後，撐到 TJ 危機的年數（體力 60、球威 par+14）：

| 聯盟 | 檔位 | 二刀 | 單刀 |
|---|---|---|---|
| 中職一軍 | 全力投 | 6.5 年 | 6.6 年 |
| 中職一軍 | 普通投 | 8.4 年 | 8.5 年 |
| 中職一軍 | 養生球 | 11.5 年 | 10.6 年 |
| 大聯盟 | 全力投 | 5.2 年 | 5.2 年 |
| 大聯盟 | 普通投 | 6.8 年 | 6.8 年 |
| 大聯盟 | 養生球 | 9.2 年 | 8.5 年 |

#### 全力投再從 1.45 拉到 1.55（v1.6.3）

上面那張表的問題是「全力投」那兩列**打平**，不是超前。局數少掉的量比倍數補回來的多，
所以二刀流全力投的手臂還是跟全職王牌一樣耐用，手術時間點只是打平而已。
`workload` 項在這個區間已經頂到 1.2 的上限，唯一的差別就只剩倍數本身
（1.45 ÷ 1.30 = 1.115），補不回來。

拉到 1.55 之後（中職一軍、體力 70、球威 65，量表累到 60 為止）：

| 檔位 | 首季 IP | 每季磨損 | 量表滿 60 |
|---|---|---|---|
| 單刀先發・全力投 | 179 | 10.67 | 6 季 |
| 二刀流・全力投（1.45） | 152 | 11.52 | 6 季 |
| **二刀流・全力投（1.55）** | 152 | **12.32** | **5 季** |
| 二刀流・普通投 | 126 | 8.79 | 7 季 |

現在全力投的二刀流會比全職王牌**早一年**走進手術室。

普通投與養生球隨後也各加了 0.1（1.25→1.35、1.10→1.20），理由一樣：局數少掉的量
本來就會自動幫他省手臂，三檔都要補回來，不然「養生球」會變成沒有代價的最優解。
普通投的每季磨損從 8.79 變 9.50，量表滿 60 從 8 季（單刀普通投）縮到 7 季。

數字現在只有一份：`season.js` 的 `TW_EFFORT.tj`（二刀流）與 `injury.js` 的
`SOLO_EFFORT_TJ`（單刀），兩邊都透過 `tjEffortMult()` 取用。

原本**三個地方各寫一份**：`season.js` 的 `TW_EFFORT.tj`（其實從來沒被讀過）、
`injury.js` 裡的行內表（真正生效的那份），以及 `flow/phases.js` 的
「開季投球規劃」面板文案。第一次調 1.45→1.55 時只改了前兩個，
玩家在面板上看到的還是 ×1.45——引擎已經改了，顯示沒改。
現在面板直接讀 `tjEffortMult()`，不可能再各說各話。

一句話講完：**全力投與普通投時，省下來的局數剛好被「沒有休息日」抵銷掉，
磨損率和全職王牌一模一樣；只有養生球才真正買得到休息。**

（更貼近大谷本人的一組是 1.50 / 1.35 / 1.20，二刀流的手臂比單刀更短命。
沒有採用，是因為這個角色已經在能力點上付過一次平衡的代價，再讓手臂比別人早壞，
容易變成「玩起來只有痛苦」。）

---

## 6. 七下觸發

**強制二刀流，不是天才速成器。**

- 標題 `#logo-tap`（`index.html:46`，目前全專案零引用）連點 **7 下** → `selPos` 鎖成 `'TW'`，
  守位選單 `#seg-pos` 換成單一個「二刀流」，玩家**不能**改選投手／捕手／內野／外野。
- 這樣它就不會變成「刷天才給一般守位用」的後門。
- **再連點七下關閉**，還原成開啟前選的那個守位（不是還原成預設的投手）。重整頁面也可以。
- **不寫 localStorage**：每一局都要重點。
- 連點的判定：兩下之間超過 1.5 秒就重新計數，避免慢慢誤觸也會累積到七下。

**骰子配額：只保送 3 顆，剩下兩顆要自己擲。**

常數是 `TW_SIX_GUARANTEED`（`flow/phases.js`），這是整條路線的難度總開關。

- 高中三年的**累積目標**＝`ceil(G × 年 / 3)`。G=3 → 高一 1、高二 2、高三 3。
- 每季開骰時先看 `S.six` 差多少才到當季目標，缺幾顆就在骰陣中隨機挑幾個位置固定為 `6`；
  **其餘骰子照常擲 `ri(1,6)`**——自然骰出的 6 會算進去，下一季要補的就變少。
- 天才仍照現有邏輯在 `S.six>=5` 時解鎖，計數器全程可見。解鎖後 `S.six` 停止累加，
  配額邏輯自動失效，不需要額外的關閉條件。

**為什麼不是全保送**：這個入口會公開公告，不是私藏彩蛋，所以整包必須接近母體公平。
實測（各 3000 段完整生涯，名人堂率；單刀母體 8.3%）：

| 保送顆數 | 天才發生率 | 名人堂 | 明星以上 | 邊緣+過客 |
|---|---|---|---|---|
| 0 顆 | 55.9% | 6.9% | 27.9% | 30.3% |
| 2 顆 | 80.6% | 11.3% | 37.4% | 22.5% |
| **3 顆（採用）** | **88.0%** | **12.2%** | **40.0%** | **20.0%** |
| 4 顆 | 97.5% | 15.9% | 47.3% | 14.9% |
| 5 顆（全保送） | 100% | 18.1% | 51.6% | 13.0% |

全保送是母體的 2.2 倍，太甜；完全不保送則天才發生率只剩 55.9%，
拿不到天才的二刀流名人堂率只有 2.6%，這條路線會變成「賭到天才才玩得起來」。
3 顆讓天才發生率 88%、整包 1.47 倍母體——**進得去、但不是免費的**。

### 觸發 B：高中解鎖天才後的邀請

解鎖天才的那一年若還在高中且不是二刀流，跳出一次詢問；婉拒之後（`S.twDeclined`）不再問。
收到邀請的比例約 **3.6%**（高中就湊滿五顆 6 本來就少見）。

**新增那一側的起點是關鍵，這裡差一點整個選項就變成陷阱。**

第一次校準（2026-09-05）解出 ×0.85。但那一輪的底層數據帶著 §8 的 WHIP 欄位衝突
（見下面「第 2 階段的欄位衝突」），二刀流的投球側被系統性低估，數字整組偏低。
修掉之後重量（同一批收到邀請的人，n=253，名人堂率）：

| 新增能力的起點 | 名人堂 | 明星以上 | 每日先發 | 邊緣+過客 |
|---|---|---|---|---|
| 從頭擲 `ri(20,32)` | 6.6% | — | — | — |
| 現有平均 × 0.85 | 13.4% | 36.4% | 39.9% | 10.3% |
| 現有平均 × 1.00 | 13.8% | 39.1% | 38.3% | 8.7% |
| **現有平均 × 1.15（採用）** | **17.0%** | 36.4% | 36.0% | 10.7% |

對照：七下型 16.5%（拿到天才的母體 18.3%）。**×1.15 才讓兩個入口對齊**——
怎麼走進來的不該改變難度。

×1.15 看起來像「憑空變強」，其實不是：分母是**全部**能力的平均，裡面含著
`rng`/`fld`/`arm` 這些他轉型後再也用不到的守備工具，會把平均往下拉。
×1.15 之後新增的那一側大約落在他現有核心工具的水準，不會高過去。

**接受仍然比婉拒低（17.0% 對 26.5%），而且這個差距是刻意留著的。**
會收到邀請的是「高中就解鎖天才」——母體裡最強的一群，他們維持單刀本來就有 26.5%。
二刀流是更難的路（見 §6 開頭：機制本身 6.8%，比單刀母體 8.3% 還低），
硬把接受拉到 26.5% 等於讓二刀流變成 HS 天才的免費升級。
所以這是一筆誠實的取捨：拿生涯評價的機率，換二刀流這件事本身
（專屬牌庫、兩份里程碑、同年雙獎）。

另外兩件事：
- **體力潛力墊到 TW 下限**（`max(原值, ri(60,74))`）。單刀的潛力表把體力丟進洗牌，
  過半的人抽到 `ri(44,54)`／`ri(46,62)`——照原值轉過來的話這個邀請對他們是死路。
- **原有的守備工具保留**（`rng`/`fld`/`arm`/`cat`）。它們只是變成用不到，不是被沒收；
  日後被強制轉回打者時還會派上用場。

---

**失去二刀流身分 → 天才一併拔除。**

七下是一條「免費領天才」的捷徑，若失敗後還能留著天才去打純投手／純野手，
那條捷徑就會變成最優解。所以：

- 用 `S.twOrigin`（`'tap'` / `'genius'`）記錄成為二刀流的來源。
- 強制轉回時，若 `S.twOrigin === 'tap'` → `removeTrait('genius','天才')`。
  有機路線（自己擲出五顆 6）**不拔**——那是憑實力拿到的。
- **必須同時把 `S.six` 歸零。** 解鎖條件是 `S.six>=5 && !S.traits.genius && S.age<22`，
  而 `S.six` 已經是 5——只要玩家未滿 22 歲，下一次擲骰就會立刻再跳一次解鎖卡，等於白拔。
  這是構得到的：七下玩家高中就解鎖、18~22 歲進職業，強制轉回完全可能發生在
  中職二軍（下限 20）的 20~21 歲。
- 歸零之後他仍可在 22 歲前用 `ri(1,6)` 再擲出五顆 6 拿回天才——機率極低，
  但門留著，比永久封鎖好聽，也少一個要維護的旗標。
- `removeTrait()`（`events.js:11`）現成，會把標籤丟進 `S.removed[]` 以刪除線顯示。

---

## 7. 生涯評價與名人堂（最難的一塊）

`careerScore()`（`career.js:66-69`）就一行 `if(S.pos==='P')`。投手分與打者分是**分開校準**的
（打者那條還乘了 0.67 做跨位校正），而 `TIER_TH` / `HOF_TH_K` / `LEAGUE_K`
都是用母體模擬反解出來的常數。

**直接把兩邊相加，二刀流會變成無腦進名人堂；不加又完全不划算。**

需要：
- `tierOf()` 的 `posKey` 新增 `'TW'`，在 `data/economy.js:87,118` 補 `LEAGUE_K.TW` 與 `HOF_TH_K.TW`
- `honorScore()` 現在是「每年取最高一項」——二刀流同年拿下最佳投手＋最佳打者會被吃掉一座，要放行
- TW 的守位調整**不要沿用 DH**（`POS_ADJ_RUNS.DH = −14`、`POS_TIER_K.DH = 1.13`，全聯盟最重的懲罰）；
  沿用會讓二刀流被罰兩次

### 校準結果（2026-09，已完成）

工具：`tools/build-headless.mjs`（把 `src/` 轉成可在 node 跑的無畫面副本）
＋ `tools/sim-twoway.mjs`（完整生涯模擬，玩家行為由母體模型驅動）
＋ `tools/analyze-twoway.mjs`（離線換常數重算，不必重跑模擬）。
樣本 18,000 段完整生涯（六條路線 × 3,000）。

**錨點為什麼是 18% 而不是全母體的 15%**：二刀流一定有天才（七下保送五顆 6），
所以對照組必須也是拿到天才的人。同一批模擬裡拿到天才的單刀路線：

| 路線 | 名人堂率（限拿到天才者） |
|---|---|
| 指定打擊 | 27.1% |
| 游擊 | 20.3% |
| 中外野 | 18.6% |
| 先發投手 | 15.2% |
| 一壘 | 10.2% |
| **二刀流（校準後）** | **18.1%** |

落在這群人的中間——比一壘與先發投手好，跟中外野、游擊相當，比指定打擊差。
既不是支配解，也不是廢棒。

⚠ 上表是在「七下全保送五顆 6」的前提下解出來的，而且底層數據帶著第 2 階段的
欄位衝突（見下面）。兩件事都修掉之後（保送 3 顆 ＋ 投打欄位分家），**重量的結果**：

| 路線 | 全母體 | 限拿到天才者 |
|---|---|---|
| **二刀流** | **16.5%** | **18.3%** |
| 指定打擊 | 12.4% | 27.2% |
| 捕手 | 11.4% | 22.0% |
| 游擊 | 9.9% | 18.8% |
| 中外野 | 8.6% | 18.0% |
| 先發投手 | 7.3% | 16.9% |
| 一壘 | 4.4% | 10.3% |

拿到天才的二刀流是 **18.3%**，正中原本設定的 18% 錨點，而且落在同條件單刀路線
（10.3%～27.2%，平均 18.9%）的中間。**係數不用重解**——先前量到的 12~13% 是
欄位衝突造成的低估，不是尺歪掉。

全母體那一欄二刀流看起來領先很多（16.5% 對單刀平均 9.0%），那是因為二刀流一定有天才；
對等比較請看右欄。

**常數**：`careerScore` 對二刀流是「投手分＋打者分」相加，不在分數端打折，
全部交給 `LEAGUE_K.TW`。形狀取同聯盟投手與打者的平均（P/H 之間的聯盟關係是
v1.6.0 校準過的，不該重新發明），再乘一個整體倍率 0.785：

```
LEAGUE_K.TW  CPBL [1.428, 0.339]   NPB [1.574, 0.396]   MLB [2.296, 1.205]
HOF_TH_K.TW  三個聯盟都是 1
```

要調難度只動倍率：15% → ×0.737、20% → ×0.817、25% → ×0.901。

### 第 2 階段的欄位衝突（2026-09-06 才發現）

第 2 階段把「投球場次」搬到 `st.GP`，但**被安打 `H` 與四死球 `BB` 忘了一起搬**。
二刀流一季會先跑投球側寫 `st.H`（被安打），再跑打擊側用自己的安打**蓋掉它**。
兩個後果：

- `WHIP = (H+BB)/IP` 變成「(自己敲的安打＋自己被保送)÷投球局數」。實測一季
  1.81 對正確值 1.01——`pitcherQualityFactor` 直接吃這個數，二刀流的投球分被系統性低估。
- `applySeasonForm` 的生涯年／低潮倍率，投手側 `÷m` 之後打擊側 `×m` **互相抵銷**，
  等於兩側的狀態調整都失效。

同時 `normalizePitchingStats` 是靠 `Number.isFinite(st.GP)` 判斷「這是不是二刀流」，
而第 2 階段讓**每個投手**都寫了 `st.GP`——所以每個純投手都被判成二刀流，
`st.G` 從此永遠是 0。逐年表、後援出賽上限、鐵人判定全部讀到 0，
先發投手的名人堂率被壓到 7.3%（修好之後 16.9%，與 v2.0.2 逐格相同）。

修法：投球側三個欄位一起依身分分家（`gK`/`hK`/`bbK`），單刀投手照舊寫
`st.G`/`st.H`/`st.BB`，二刀流寫 `st.GP`/`st.pH`/`st.pBB`；讀取一律走
`pitG()`/`pitH()`/`pitBB()`。`tests/twoway_phase5.mjs` 兩邊都釘住了。

### v1.6.1 之後的重新校準

聯盟環境常數化（見 `docs/league-environment.md`）把率值基準改成隨聯盟走之後，
`LEAGUE_K` 全部重解過一次。二刀流的常數形狀維持這裡訂下的「同聯盟投手與打者的平均」，
只有整體倍率改變；名人堂率從 15.9% 回到 15.6%，等於沒動。

### 兩個實作時才量出來的東西

**① 二刀流不是「早夭」，是「壽終」。** 89.5% 的二刀流會被強制轉回單刀，
但**轉回年齡的中位數是 39 歲，此時已經打了 19 個二刀流球季**；
不到 5 季就崩掉的比例是 **0.0%**。所以規則 5 實際上是「投球生涯的自然終點」，
不是懲罰——跟真實世界的二刀流球員一模一樣。

**② 因此天才連坐必須加一道「是否已經站穩」的門檻。** 對照組實測：
故意只練投球側的「刷天才」玩法 **100% 在 5 季內崩掉、轉回年齡中位 19 歲**。
所以 `twSeasons < 5` 這條線抓得到 100% 的刷天才、誤傷 0% 的正常玩家
（保送降到 3 顆之後重測：強制轉回 86.8%，其中不到 5 季就崩掉的是 **0.00%**）。
沒有這道門檻的話，一個打了十九年二刀流的球員會在四十歲被拔掉天才。

**③ 生涯評價不能看 `S.pos`。** 承上，89.5% 的二刀流在結算當下的 `S.pos` 已經是
`P` 或 `OF`，用它判斷會讓整段十九年的履歷被當成純單刀計分。改成看
`isTwoWayCareer(st)`——同一個聯盟裡既有登板也有打席——依「他實際打出來的東西」判定。

---

## 8. 掃出來的風險清單

依嚴重度排序。實作時逐條劃掉。（#1 與 #6 已由 §4 的 `st.G` 分家解掉，保留說明。）

### 結構性（要重寫）

1. **`st.G` 一個欄位兩種意思。** 投手的 `G` 是先發／後援場次，打者的 `G` 是出賽數，
   `simSeason()`（`season.js:60`）只回傳一個 `st`。二刀流需要兩個獨立的出場預算，
   而且要用同一份 `sta` 去付。
2. **`ovr()`（`ability.js:120`）是全域關卡**：升降級、國際賽徵召、選秀輪次、旅外門檻、
   引退底線全部走它。TW 需要一個合併算法，否則打擊對每一道關卡都是隱形的。
3. **`dposReview()`（`ability.js:40-106`）在投手分支 `return`**，二刀流會拿不到 `dpos`。
   要改成 role 與 dpos 兩段都跑。
4. **`abCost()` / `addAb()` / `addAbStat()`（`ability.js:154,179,199`）用 `const isP=S.pos==='P'`
   決定成本曲線**——要改成**看 key**，不是看 pos：`vel/ctl/brk` 走投手陡曲線，
   `con/pow/eye/spd` 走打者曲線，`sta` 走打者曲線。同一條公式抄了三份，改的時候三份都要同步。
5. `seasonSalaryRating()` / `seasonGrade()`（`season.js:216,243`）各自 `if/else` 回傳一個數字，
   沒有地方把投球價值＋打擊價值加起來。`seasonGrade` 還驅動升降級與明星級年資。

### 會安靜出錯（最危險）

6. **`tjAccrue()`（`injury.js:10`）用 `isSP()?st.IP:st.G` 當工作量**——二刀流的 `st.G`
   含打擊出賽，後援累積會被灌爆。**一定要換成獨立的投球出場計數。**
7. **季前衰退 `phases.js:27-36` 是 `POS_AB[S.pos].forEach`**——沿用 `POS_AB.P` 的話
   二刀流的打擊永遠不會衰退。`injStatLoss()`（`injury.js:155`）同理。
8. **`eventTarget()`（`events.js:71`）** `ev.target in S.ab ? ev.target : pick(POS_AB[S.pos])`
   ——打擊卡的 `target:'con'` 若 `S.ab` 沒有該鍵會靜默 fallback 成隨機投手屬性。
9. `accStat()`（`season.js:314`）的 `else if` 讓 `roleYears` 與 `dposYears` 只會累積一邊。

### 閘門放寬（改條件、邏輯不動）

10. `eventEligible()`（`events.js:36-49`）：`role==='B'` 排除 P 和 C、`role==='F'` 排除 P。
    TW 要能同時吃投手卡與打者卡。改完要驗 `availableEventCombinations()` 三條路線都還湊得出組合。
11. `awards.js` 的兩大區塊分別包在 `if(S.pos==='P')`（124-166）與 `if(S.pos!=='P')`（167-235）。
    拆開後**各側的工作量門檻要保留**，否則二刀流會用半套出場數領雙獎。
    `rookieWorkloadEligible()`（33-41）與 `mvpQual`（237-271）也要 TW 分支。
12. `termParams()`（`contract.js:429`）與 `faMarket()`（`contract.js:564`）
    各有一份 `S.pos==='P'?pitcherContractCap():15`——建議 TW 走 15（大谷那張是 10 年）。**兩處都要改。**
13. `movement()`（`phases.js:311-326`）的降級保護只看一側，要改成任一側夠好就保護。
14. ~~`intl.js:79-104` 的 `intlSt` 二選一，要合併。~~ **（已合併：二刀流在國際賽兩邊都上，
    投球走先發線、打擊照樣每場先發；`intlStatLine`／`intlMvpRate` 同步加了 TW 分支。）**
14b. **`proSeason` 的火燙／低潮原本是 `if(投手)…else if(打者)…`**，二刀流會落進打者那一支，
    投球側整季的加成與折損通通不見。已改成兩側各自判斷。
14c. **`H` / `BB` 跟 `G` 一樣是一個欄位兩種意思**——第 2 階段只搬了 `G`，
    漏掉這兩個。後果與修法見 §7「第 2 階段的欄位衝突」。

### 顯示層（廣但淺）

15. `ui/alloc.js:49` `POS_AB[S.pos]` 決定顯示哪幾列——8 列排版要看一下。
16. ~~`ui/dom.js` `secLog()` 六欄表頭投打二選一，行動版還有 `drop[]` 遮罩。~~
    **（已完成：六格投打各佔三格，手機收成三格時兩側各留一個率。）**
17. ~~`ui/retire.js` 四個資料建構器各自 `const isP=...` 回傳完全不同的形狀；
    再加結算表頭、四段引退結局文案、努力仔門檻。~~ **（已完成。四篇引退結局、
    老將留言、引退儀式、生涯標語、生涯檔案的手肘欄都補了 TW 分支，
    人稱統一走 `endingPos()`——同樣看實績而不是 `S.pos`。）**
18. ~~**`ui/share-image.js` 的年表欄位是硬幹在 820px 畫布上的固定像素寬**
    （投 12 欄、打 15 欄）。二刀流 ~20 欄放不進去——要改成一季兩列（投／打）或加切換。
    這是唯一一個「不是改 code 就能解決」的地方。~~
    **（已完成，而且不必兩列：捨掉對二刀流沒有意義的欄位之後是 14 欄，
    比野手的 15 欄還窄。欄位表見 §10。）**
19. `playerType()`（`ability.js:135`）要一個二刀流稱號；`dom.js:199` 的記分板 chip
    其實已經會把 `dpos` 和 `role` 串在一起（「右外野手・先發」），幾乎是現成的。

---

## 9. 事件卡：XLSX 要怎麼填

`src/data/events.js` 由 XLSX 的「完整事件表」產生，目前 **92 張，ID 1~92**（93~96 仍空缺）。
新卡請從 **ID 97** 開始編。

### 欄位

| 欄位 | 必填 | 允許值 | 說明 |
|---|---|---|---|
| `id` | ✓ | 整數 | 從 97 起。不可與現有重複。 |
| `n` | ✓ | 文字 | 卡名，會顯示在事件標題「事件｜類別｜卡名」。 |
| `times` | ✓ | `ALL` / `HS` / `U` / `AMA` / `MINOR` / `PRO`（可多選） | 可出現的生涯階段。 |
| `scope` | ✓ | `*` / `CPBL` / `NPB` / `MiLB` | 限定聯盟；`*` 為不限。 |
| `role` | ✓ | `*` / `P` / `B` / `C` / `F` / **`TW`** | **二刀流專屬卡填 `TW`。** |
| `category` | ✓ | `training` / `endorsement` / `encounter` | 訓練／代言／遭遇。 |
| `intro` | ✓ | 文字 | 引言，接在卡名下方；句尾通常不加句號（後面接選項）。 |
| `target` | ✓ | `sta`/`vel`/`ctl`/`brk`/`con`/`pow`/`spd`/`eye`/`rand` | **二刀流卡只能用這 8 項或 `rand`**——填 `rng`/`fld`/`arm`/`cat` 會靜默失效。 |
| `maxAge` | | 整數 | 選填，超過此年齡不再抽到。 |
| `note` | | 文字 | 給人看的備註，不進遊戲。 |

### 三個選項的文字

每張卡要 **9 格**：`bold` / `norm` / `safe` 各一組 `label`（按鈕文字）、`good`（成功）、`bad`（失敗）。

- `bold` 全力一搏：成功率最低、獎勵最大
- `norm` 普通應對
- `safe` 保守：成功率最高、獎勵最小

（成功率由 `eventPlan()` 統一計算，不寫在卡上。）

### 有傷病風險的選項

若某個選項的**失敗文本描述了受傷／疼痛／過勞**，要另外登記到 `EVENT_INJURY_BAD_MODES`
（`data/events.js:17-23`），格式 `卡ID:['bold','norm','safe' 之中會受傷的]`。
XLSX 裡請在 `note` 欄註明「傷病：bold, safe」之類，我來搬進表裡。

### 建議張數

現有各類別可用卡數（`*` 為所有人都抽得到）：

```
training    * 9 ｜ P 18 ｜ B 10 ｜ C 5 ｜ F 1
endorsement * 10 ｜ B 4          （沒有投手專屬代言卡）
encounter   * 17 ｜ P 4 ｜ B 7 ｜ C 7
```

二刀流會同時吃到 `*` ＋ `P` ＋ `B`，牌庫本來就夠厚，
所以 **`TW` 專屬卡是拿來做味道的，不是拿來補數量的**——
每個類別 2~4 張就夠，重點放在只有二刀流會遇到的處境
（休息日要不要上場打、投完隔天的打擊、兩邊教練搶人、被質疑分心、專守一邊的誘惑……）。

---

## 10. 實作階段

1. ~~**能跑**~~ **（完成，`3e4d0f3`）** — `POS_AB.TW`、`newState` 的能力／潛力產生、
   `ovr()`／`ovrPit()`／`ovrBat()`、`abCost` 改成看 key、配點 UI 8 列、
   `dposReview` 同時給 role 與 dpos、七下入口與骰子規則。測試 `tests/twoway_phase1.mjs`。
2. ~~**能玩一季**~~ **（完成）** — `simSeason` 雙成績線（`st.GP` 分家）、
   `seasonSalaryRating`／`seasonGrade` 拆成兩側再合併、投打配比面板、
   TJ 倍數與只停投球的復健年、強制轉回與天才連坐。測試 `tests/twoway_phase2.mjs`。
   ~~**仍未做**：`intl.js` 的國際賽只會產生打擊成績；`proSeason` 的狀態火燙只加打擊側~~
   **（兩項都在第 4 階段補完；同時發現並修掉 `H`/`BB` 的欄位衝突，見 §7）**
3. ~~**能校準**~~ **（完成）** — `careerScore` 兩側相加、`isTwoWayCareer()` 依實際成績判定、
   `posTierK` 對二刀流回 1、`honorScore` 放行同年雙獎、`LEAGUE_K.TW` 反解完成。
   另外補上「已站穩才不連坐」的天才門檻。測試 `tests/twoway_phase3.mjs`。
4. ~~**能看**~~ **（完成）** — ~~事件卡~~、~~七下觸發~~、~~觸發 B~~、~~結算版面~~、~~引退文案~~。
   事件卡不需要新增資料：二刀流抽的是投手卡與打者卡的**聯集**，中職一軍實測 17／7／11，
   比純投手（14／6／8）與內野手（10／7／10）都厚，四條路線在每個職業層級都湊得出來。
   測試 `tests/twoway_phase5.mjs`。

### 結算版面：投打各自一張表

**先試過「擠成一列」，那條路走不通。** 投打兩側的完整欄位聯集是二十欄，結算圖的畫布
只有 820px（單刀最寬的野手表已經佔掉可用寬度 748 的 680），所以曾經訂了一組
「兩側各留必要欄」的表，砍掉投手側的 `SV`/`HLD`/`BB`/`WHIP` 與打者側的
`OBP`/`SLG`/`H`/`BB`/`SB`/`DEF`。結果是兩邊都殘缺，而且同一列上兩組數字混在一起，
讀的人得先想「這個 G 是登板還是出賽」。

**改成拆表：一張投球、一張打擊，每一張都用回單刀的完整欄位。**
二刀流反而看得比單刀還完整——他有兩份完整的成績單，而不是一份被砍半的。

| 畫面 | 單刀 | 二刀流 |
|---|---|---|
| 球季數據卡 | 一個框 | **兩個框**（投／打），同一張卡 |
| 生涯累積數據 | 一張表 | 兩張（`大聯盟・投球`／`大聯盟・打擊`） |
| 生涯年表（職業） | 一張表 | 兩張（`職業成績・投球`／`・打擊`） |
| 國際賽逐屆 | 一張表 | 兩張 |
| 合約薪資與成績 | 一張表 | 兩張 |
| 逐年板（遊戲中，只有六格） | 六欄 | **點一下切投／打**，各自是該側完整的六欄 |

逐年板是一條六格寬的窄帶，塞不下兩側。曾經是「投打各佔三格」，等於兩邊都被砍半，
而且同一列上兩組數字混在一起。改成切換之後，切到哪一側就用回**單刀的完整六欄**
（投球 `G IP W-L SV SO ERA`／打擊 `G PA AVG HR RBI OPS`），
手機收成三格的遮罩也直接沿用單刀那一組，不必另外訂。

切換按鈕掛在「職業」那一列的右邊，不掛在區塊小標上——小標（`.bd-sh`）在手機版是
`display:none`（手機用分頁列取代標題），掛上去玩家就看不到了。實測 390px 寬時
按鈕組寬 78px、離右緣 16px，逐年列沒有溢出。
選到哪一側記在面板的 `dataset.yside`，`detailSync()` 重畫時沿用，
切分頁或 `board()` 刷新都不會被打回投球側。

三件必須這樣做的事：
- **版面判斷用 `twoWayView()`，不是 `S.pos`。** 跟計分同一個理由：89.5% 的人退休前
  已被強制轉回，看 `S.pos` 會讓十九年的履歷用單刀版面印出來，缺的那一側整段消失。
- **沒上場的那一側整列不畫。** 轉型前後的單刀球季只會出現在它該在的那張表裡，
  不留一排 `-`。
- **薪資表是例外**：沒登板的那一年還是有領薪水，所以照樣列出來，只有該側的成績印 `-`。

## 薪水：核薪與評價分開（v1.6.3）

「二刀流薪水有點低」量下去之後，發現不能直接比路線中位數——同樣的點數預算，
二刀流要分給投打兩邊，`peakCore` 中位是 62.8，指定打擊是 66.3，他本來就是比較弱的球員。
對齊同一個 `peakCore` 帶再比生涯收入（只看有站上日職一軍以上的）：

| peakCore | 二刀流 ÷ 指定打擊（改前） | 改後 |
|---|---|---|
| 55~62 | 1.59 | **1.84** |
| 62~68 | 1.13 | **1.51** |
| 68~75 | **0.93** | **1.16** |

低段與中段本來就領先（一個邊緣的指定打擊幾乎沒有價值，邊緣的二刀流還能吃局數）。
壞掉的是**頂端**：頂級二刀流領得比頂級指定打擊還少。現實裡大谷的合約是頂級純打者的
1.75 倍。兩個原因：

**① `salaryFor` 的轉折點（`contract.js`）。** `d > 15` 只計 35%。二刀流合成之後幾乎
一定落在 15 以上，於是他多做的那一份工作**整份**掉進被壓縮的區間。而中職那條公式
根本沒有轉折點，所以症狀只出現在日職與大聯盟——正好是二刀流最該被付錢的地方。
二刀流的轉折點改成 **22**。

**② 核薪與評價共用同一個合成係數（`season.js`）。** `twoWayD` 的 0.35／上限 6
是為「他有多強」校準的，但薪水問的是「他幫球隊省下幾個名額」，是兩件事。
拆成 `twoWayPayD`（**1.00／上限 22**）之後，生涯評價那條線完全不動——
二刀流的名人堂率本來就是七條路線裡最高的（15.0%），不能再加。

係數是掃出來的（對齊 peakCore 帶的中位倍數）：

| 係數 | 55~62 | 62~68 | 68~75 |
|---|---|---|---|
| 0.35（原本） | 1.59 | 1.13 | **0.93** |
| 0.55 | 1.66 | 1.24 | 1.01 |
| 0.80 | 1.78 | 1.39 | 1.10 |
| **1.00** | **1.88** | **1.52** | **1.20** |

試過只推頂端而不動低段（讓係數隨弱側大小遞增），**沒有用**：頂端那一帶的弱側其實
不大——點數要分給投打兩邊，`peakCore` 70 的二刀流兩側各自都只有中段水準，
遞增段根本沒被觸發，斜率 0.030 與 0.055 量出來一模一樣。轉折點也一樣：
把 22 拉到 26、30、甚至整個拿掉，頂端那一帶的中位收入一毛都沒動。
**能動頂端的只有整體係數**，而它會等比例把三段一起抬。

所以取 1.00——「兩份工作就是相加」，也是最不需要解釋的版本。低段變甜
（1.88 倍）是可以接受的：一個邊緣的指定打擊幾乎沒有價值，邊緣的二刀流還能吃局數，
而且那一段的絕對金額本來就很小（4.1 億對 2.2 億）。

改動後級距分布一格都沒動（旅外名人堂 10.0%、只留中職 6.2%，與改前完全相同），
變的只有錢。二刀流現在是七條路線裡收入最高的：

| 路線 | 生涯收入 p50 / p90 / p99（億） |
|---|---|
| **二刀流** | **6.4 / 18.8 / 36.9** |
| 指定打擊 | 4.9 / 15.0 / 23.4 |
| 游擊 | 1.8 / 10.5 / 18.1 |
| 先發投手 | 1.2 / 8.3 / 16.4 |

## 狀態火燙／低潮：憑空生局數的 bug（v1.6.3）

回報的案例：能力 **球速 53／控球 46／變化球 56** 的二刀流，在**大聯盟**（par 59，
三項全部低於聯盟平均）投出 **6 登板／46.0 局／ERA 1.76／63 三振**——
每場 7.7 局、K/9 12.3。兩個都是不存在的數字。

成因在 `proSeason` 的狀態火燙加成：

```js
st.SO += Math.round(p*8);  st.IP = +(st.IP + p*4).toFixed(1);
```

兩個問題：

1. **加成是絕對值，不隨工作量縮放。** 投 200 局的王牌加 8 個三振無感；只投 30 局的
   二刀流加 8 個就是 +30%。**二刀流被放大得最嚴重**，而且加的三振÷加的局數
   隱含 18.0 的 K/9。
2. **憑空生出來的局數沒有對應的被安打與四死球。** v1.6.1 把 ERA 改成由零件反推之後，
   `IP` 是 `h9`／`bb9`／`hr9` 的共同分母——多灌 16 局進去，三個率值同時被除小三成，
   ERA 直接崩到 1 點多。舊版看不出來，因為那時候 ERA 是獨立擲的，跟被安打無關。
   而且 `登板數` 沒有跟著加，才會出現每場 7.7 局。

`p`（＝`pendStat × seasonFactor`，來源是能力點打到潛力上限後的溢出）的實測分布：
中位 1.0、p90 2.1、p99 3.0、最大 9.0。回報那一季反推是 p ≈ 4。

### 改法

拆成兩件事，兩件都是**比例**（`season.js` 的 `formPitch`／`formBat`）：

- **出賽機會**：火燙＝教練多用你。先發加先發場次（上限＝該層級場次 ÷ 5 × 1.12），
  後援加登板。局數跟著自己的「每場局數」走，不會憑空出現。
- **內容**：直接動 `k9`／`h9`／`bb9`／`hr9` 四個率值，再由 `IP` 反推 counting stat，
  最後 `syncEra`。

打擊側同樣的病：舊版新增的打數是用固定 .550 打擊率再加 `p×1.5` 支安打填的，
實測那批新打數的打擊率會到 .8 以上。改成新打數用他自己的打擊率，火燙再給一個比例加成。

修完之後同一組能力（大聯盟、`pendStat` 0→9）：

| pendStat | 登板 | 局數 | 每場局數 | K/9 | ERA | WHIP |
|---|---|---|---|---|---|---|
| 0 | 13 | 62.3 | 4.79 | 7.99 | 5.01 | 1.51 |
| 3 | 15 | 71.9 | 4.79 | 8.76 | 4.45 | 1.40 |
| 9 | 18 | 86.3 | 4.79 | 10.01 | 3.53 | 1.27 |
| −3 | 12 | 57.5 | 4.79 | 7.36 | 5.35 | 1.54 |

每場局數 4.79 從頭到尾不動，ERA 與 WHIP 同進同退。

### 連帶：LEAGUE_K 再解一次

投手側的原始分整片下修，**二刀流掉最多**（名人堂率 15.5% → 11.9%，因為他的局數基數
最小、被那個絕對值加成放大得最嚴重）。也就是說之前校準的二刀流名人堂率有一部分是
這個 bug 撐出來的。用 `tools/recalibrate-k.mjs` 重解回設計錨點，
「留中職刷數據」的約束一併帶著（旅外 10.0% / 只留中職 6.2%，比值仍是 0.62）。

回歸測試：`tests/season_form.mjs`。

## 收斂門檻從 min−10 拉到 min−3（v2.0.2）

寫完兩篇「衰退前放棄二刀流」的失敗結局之後才發現：**它們幾乎永遠不會出現**。
實測 1,980 段二刀流生涯，只有 **0.3%** 在衰退前被收斂。

原因是 `twoWayAudit()` 的門檻 `bar = L.min − 10` 訂得太鬆。日職一軍的 `min` 是 50，
所以只要兩側都在 40 以上就不會被收斂；而弱側 `ovr` 的中位數是 **min+5.8**，
那條線根本碰不到。95% 的人最後還是被收斂了，但年齡中位是 **39 歲**——
那不是「二刀流失敗」，那是老了。

### 實測曲線

衰退前（<32 歲）就被收斂的生涯比例：

| bar | 比例 |
|---|---|
| min−10（原本） | 0.3% |
| min−6 | 1.2% |
| min−5 | 2.9% |
| min−4 | 5.9% |
| **min−3（現在）** | **15.3%** |
| min−2 | 32.6% |
| min−0 | 76.4% |

曲線在 −3 到 0 之間非常陡（每收緊一點就多一倍），所以這個數字不能隨便動。
`min−0` 等於要求弱側單獨也能站穩該層級，那是在宣告二刀流不可行。

### 失敗長什麼樣子

改後實測（n=2,951）：衰退前收斂 **14.4%**，年齡 p25/p50/p75 是 **22／22／23**，
**79% 崩在日職一軍**。也就是說：強側把他拉上了一軍，弱側跟不上新層級的水準。

那正是二刀流該失敗的樣子，也剛好對得上失敗結局裡的那句「你試了四年」。

三個結局的實際抽中比例：

| | 改前 | 改後 |
|---|---|---|
| 〈全部的棒球〉 | 99.8% | **85.6%** |
| 〈另外那一半〉（留投） | 0.0% | **10.2%** |
| 〈我留下的那一半〉（留打） | 0.2% | **4.2%** |

留投比留打多一倍，因為 `keepPit = ovrPit() >= ovrBat()`，而二刀流的打擊側
是照指定打擊計價的（`ovrBat()` 把守備權重壓在 26），天生比投球側低一截。

### 連帶：天才會跟著一起失去

衰退前的失敗幾乎全部落在前 5 個職業季，正好撞上「5 季內崩掉就連坐拔掉七下路線
送的天才」那條規則。專案擁有者的決定是**不動**——失敗就是失敗，連天賦一起拿走。

實測：改後有 12.7% 的二刀流天才在引退前失去天才（改前是 0%）；
衰退前被收斂的 426 人裡，329 人（77%）連天才一起失去。

### 副作用：二刀流的名人堂率反而上升

14.4 → 17.5%。因為 22 歲被收斂之後，他剩下的十幾年是專心一側的單刀球員，
生涯評價改走 `P`／`H` 的係數，而且他本來就是強側夠好才被拉上一軍的。
「早點放棄反而評價更好」聽起來彆扭，但那正是兩篇失敗結局在講的事——
專心之後打出生涯新高。玩家沒有選擇權（收斂是強制的），所以不構成可以利用的漏洞。

其餘六條路線的級距分布完全沒動（誤差 0.2pp 以內），
「留中職刷數據」的約束也還在（旅外 10.4% / 只留中職 6.3%）。
